const Invoice = require('../Models/Invoice')

module.exports = {
  all: (req, res) => {
    // Find all invoices belonging to the tenant
  },
  find: (req, res) => {
    // Find one invoice belonging to the tenant
  },
  create: (req, res) => {
    // Create an invoice for the tenant
  }
}
